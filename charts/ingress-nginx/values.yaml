ingress-nginx:
  fullnameOverride: ingress-nginx
  controller:
    kind: Deployment
    watchIngressWithoutClass: true
    service:
      enabled: true
      externalTrafficPolicy: "Local"
      #healthCheckNodePort: 32596
      annotations:
        external-dns.alpha.kubernetes.io/hostname: "av0.de."
    metrics:
      enabled: false
      serviceMonitor:
        enabled: false
